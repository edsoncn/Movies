<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/item_sort" android:title="@string/menu_sort"
        android:orderInCategory="100" app:showAsAction="always" >
        <!-- "file" submenu -->
        <menu>
            <group android:checkableBehavior="single">
            <item android:id="@+id/item_popularity"
                android:title="@string/menu_sort_by_popularity"/>
            <item android:id="@+id/item_rate"
                android:title="@string/menu_sort_by_rate"/>
            </group>
        </menu>
    </item>
</menu>
